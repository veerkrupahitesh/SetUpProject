<manifest xmlns:android="http://schemas.android.com/apk/res/android"

    package="com.setupproject.hitesh">

    <application
        android:name="com.setupproject.hitesh.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name="com.setupproject.hitesh.activity.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.setupproject.hitesh.activity.SignInActivity" />
        <activity android:name="com.setupproject.hitesh.activity.ForgotPasswordActivity" />
        <activity android:name="com.setupproject.hitesh.activity.HomeActivity" />
    </application>

</manifest>
